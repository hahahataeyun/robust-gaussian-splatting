name: gaussian_splatting
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - cudatoolkit=11.8
  - plyfile
  - python=3.10
  - pip
  - pytorch=2.0.*
  - tqdm
  - pip:
      - opencv-python
      - joblib
